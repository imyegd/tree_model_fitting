# 项目拆分完成说明

项目已成功拆分为两个独立的项目：**beam_current**（束流数据）和 **beam_position**（束位监测数据）。

## 项目结构

### beam_current/ (束流数据项目)
```
beam_current/
├── src/                    # 源代码
│   ├── linear.py          # 线性回归
│   ├── tree_model.py      # 树模型
│   ├── mlp.py             # MLP
│   ├── LSTM.py            # LSTM
│   ├── PLS.py             # PLS
│   └── data_process.py    # 数据处理
├── data/                   # 数据目录
│   ├── raw/               # 原始数据
│   └── processed/         # 处理后的数据
├── models/                 # 模型文件
├── result/                 # 结果输出
├── requirements.txt        # 依赖文件
└── README.md              # 项目说明
```

### beam_position/ (束位监测数据项目)
```
beam_position/
├── src/                    # 源代码
│   ├── common/            # 共享工具
│   │   └── utils.py
│   ├── 01_data_prep.py   # 数据预处理
│   ├── 02_baseline_models.py  # 基线模型
│   ├── position.py        # 位置处理
│   ├── plot.py           # 可视化
│   ├── XGboost.py        # XGBoost
│   └── PLS_test.py       # PLS测试
├── data/                   # 数据目录
│   ├── raw/               # 原始数据
│   └── processed/         # 处理后的数据
├── models/                 # 模型文件
├── result/                 # 结果输出
├── requirements.txt        # 依赖文件
└── README.md              # 项目说明
```

## 使用方法

### 束流数据项目 (beam_current)

1. **进入项目目录**
   ```bash
   cd beam_current
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **运行代码**
   ```bash
   # 数据分析
   python src/data_process.py
   
   # 训练模型
   python src/linear.py
   python src/tree_model.py
   python src/mlp.py
   python src/LSTM.py
   python src/PLS.py
   ```

### 束位监测数据项目 (beam_position)

1. **进入项目目录**
   ```bash
   cd beam_position
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **运行代码**
   ```bash
   # 数据预处理（必须先运行）
   python src/01_data_prep.py
   
   # 训练基线模型
   python src/02_baseline_models.py
   
   # 其他工具
   python src/XGboost.py
   python src/PLS_test.py
   python src/plot.py
   ```

## 路径说明

所有路径都已更新为相对于项目根目录的路径：
- 数据文件：`./data/raw/` 和 `./data/processed/`
- 结果输出：`./result/`
- 模型文件：`./models/`

## 注意事项

1. **独立运行**：两个项目完全独立，可以分别打开和运行
2. **路径设置**：所有路径都是相对于各自项目根目录的
3. **依赖安装**：每个项目都有自己的 `requirements.txt`
4. **数据文件**：确保数据文件在正确的目录下

## 已完成的工作

✅ 创建两个独立的项目目录结构
✅ 复制所有相关代码文件
✅ 复制所有相关数据文件
✅ 复制所有相关结果文件
✅ 更新所有文件路径引用
✅ 修复导入路径问题
✅ 创建独立的 requirements.txt
✅ 创建独立的 README.md
✅ 创建必要的 __init__.py 文件

## 测试建议

建议分别测试两个项目：

1. **测试束流数据项目**
   ```bash
   cd beam_current
   python src/data_process.py
   ```

2. **测试束位监测数据项目**
   ```bash
   cd beam_position
   python src/01_data_prep.py
   ```

如果遇到任何问题，请检查：
- 数据文件是否存在
- 依赖是否已安装
- 路径是否正确

